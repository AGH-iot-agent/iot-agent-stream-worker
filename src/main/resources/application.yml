server:
  port: 8080

spring:
  application:
    name: iot-agent-stream-worker

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
